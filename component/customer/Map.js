import { View } from "react-native"
import { Text } from "react-native"

const Map=()=>{
    return(
        <View style={{flex:1, justifyContent:"center",alignItems:"center"}}>
            <Text> Chức năng google map sẽ được thêm vào sau</Text>
        </View>
    )
}
export default Map;